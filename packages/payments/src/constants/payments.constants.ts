/**
 * Tokens de inyección de dependencias para el módulo de pagos.
 * Mismo patrón que en @agelormini2024/auth: usamos strings como identificadores
 * para registrar y resolver providers que no son clases concretas.
 */

/** Token para inyectar las opciones de configuración del módulo */
export const PAYMENTS_OPTIONS = 'PAYMENTS_OPTIONS';
